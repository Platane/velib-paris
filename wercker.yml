
box: node

build:

  steps:

    - npm-install

    - script:
        name: build front
        code: npm run build-front

    - script:
        name: build back
        code: npm run build-back

    - npm-test
