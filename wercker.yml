
box: node

build:

  steps:

    - npm-install

    # - script:
    #     name: build-front
    #     code: npm run build

    - script:
        name: build-back
        code: npm run build

    - npm-test
